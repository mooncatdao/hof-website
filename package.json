{
  "name": "mooncats-hof-website",
  "version": "1.0.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "description": "MoonCats Hall of Fame static website",
  "scripts": {
    "build": "node scripts/dayoneclub.mjs",
    "cache:images": "node scripts/cache-mooncat-images.mjs",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev",
    "test": "node --test"
  },
  "dependencies": {
    "viem": "^2.38.5"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "wrangler": "^4.95.0"
  }
}
