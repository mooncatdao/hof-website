{
  "name": "mooncats-hof-website",
  "version": "1.0.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "description": "MoonCats Hall of Fame static website",
  "scripts": {
    "build": "node scripts/dayoneclub.mjs",
    "cache:images": "node scripts/cache-mooncat-images.mjs",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev",
    "screenshots:mobile": "node scripts/capture-mobile-screenshots.mjs",
    "sync:cat-names": "node scripts/sync-mooncat-names.mjs",
    "sync:cat-names:check": "node scripts/sync-mooncat-names.mjs --check",
    "test": "node --test"
  },
  "dependencies": {
    "viem": "^2.38.5"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "wrangler": "^4.95.0"
  }
}
